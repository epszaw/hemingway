language: node_js

node_js:
  - 10

dist: trusty

sudo: false

addons:
  chrome: stable

branches:
  only:
    - master

cache:
  yarn: true
  directories:
    - node_modules

before_install:
  - google-chrome-stable --headless --disable-gpu

install:
  - yarn install

script:
  - yarn test

notifications:
  slack: hemingwayworkspace:EE6Ff0jXRwp7toHG6n141NuX
